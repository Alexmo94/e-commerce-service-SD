spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/db_e_commerce 
    username: root
    password: haroll.mora
    driver-class-name: org.mariadb.jdbc.Driver
    
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    
    hibernate:
      ddl-auto: create-drop

logging:
  level: 
    org: 
      hibernate:
        SQL: debug